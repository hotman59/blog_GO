
  <%if com.user_id==current_user.id || com.user_id==@post.user_id%>
  <div class="col-md-9">
    <p><%= com.body %> <br> like</p>
  </div>

  <div class="col-md-1">
    <% @com=com %>
  <p id="likelinks">
    <%= render partial:'posts/clinks', object: @com %>
  </p>

    <%= link_to('delete',[com.post, com], :method =>'delete', data: { confirm: 'Voulez-vous vraiment supprimer ce commentaire?' }, remote: true) %>
  </div>
  <div class="col-md-2">

  </div>
      <% else %>

        <div class="col-md-2">

        </div>

        <div class="col-md-9">
          <p><%= com.body %> <br> like
          </div>

          <div class="col-md-1">
            <%= link_to('like',root_path) %>
            <%if com.user_id==current_user.id || com.user_id==@post.user_id%>
            <%= link_to('delete',post_comment_path(@post.id, com.id), method:'delete', data: { confirm: 'Voulez-vous vraiment supprimer ce commentaire?' }) %>
            <% end %>
          </div>
    <% end %>

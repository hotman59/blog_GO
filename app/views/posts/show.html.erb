<h1 class="title">
  <%= @post.title %>
</h1>

<p class="date">
  submitted <%= time_ago_in_words(@post.created_at) %> ago
  <%= link_to 'Edit', edit_post_path(@post)%>
  <%= link_to 'delete', post_path(@post), method:  :delete, data:{confirm: 'are you sure'}%>
</p>

<p class="body" >
  <%= @post.body %> </p>
  <p id="postcomcount">
    <%= render partial:'comments/postcetlcount' %>
</p>

<% if user_signed_in? %>
<h4 class="text-center">Réactions</h4>
<h5 class="text-center"> Commenter:</h5>
<div id="commentform">
  <%= render "comments/form"%>
</div>
<div id="bloccomments">
  <ul id="commentlist" class="list-unstyled">
<% @comments.reverse.each do |com| %>
<li class="row" id="comment<%="#{com.id}"%>">
  <%= render partial:'comments/comment', locals: {com: com} %>
</li>

  <% end %>
</ul>

</div>
<% end %>
